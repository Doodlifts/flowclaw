[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -c \"import os; port = int(os.environ.get('PORT', 8000)); import uvicorn; uvicorn.run('relay.api:app', host='0.0.0.0', port=port)\""
